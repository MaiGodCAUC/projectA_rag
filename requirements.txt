# ============================================================
# 国航内部员工智能知识助手 - 依赖清单
# Python >= 3.11（当前环境: Python 3.12 @ E:\develop\python\python312）
# ============================================================
# 分步安装指南（避免一次性全部失败）:
#   步骤1: pip install -r requirements.txt          ← 核心依赖，保证成功
#   步骤2: pip install FlagEmbedding                ← 重排序模型（依赖链重）
#   步骤3: pip install ragas                        ← 评估框架（依赖链复杂）
#   步骤4: pip install pytest pytest-asyncio pytest-cov  ← 测试工具
# ============================================================

# --- LLM & RAG 框架 ---
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0

# --- API 框架 ---
# 注意: uvicorn[standard] 的 extras (httptools, watchfiles, websockets) 本地已安装
fastapi>=0.100.0
uvicorn>=0.30.0

# --- 向量数据库 ---
qdrant-client>=1.12.0

# --- 文档解析 ---
# pymupdf 在 Windows 上需要 MSVC C++ 编译工具。Python 3.12 有预编译 wheel，
# 如果安装失败请执行: pip install pymupdf --only-binary=:all:
# 或者从这里下载 wheel: https://pypi.org/project/PyMuPDF/#files
pymupdf>=1.24.0
pdfplumber>=0.11.0
python-docx>=1.1.0

# --- 配置管理 ---
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- 中文分词 ---
jieba>=0.42.0

# --- 前端 ---
streamlit>=1.40.0

# --- 工具 ---
python-multipart>=0.0.10
httpx>=0.28.0
